# Example .env for the CCTV Clinic Tracker
# Copy this to `.env` and set real values before running the app.

# MongoDB connection string (standard format). Example using MongoDB Atlas:
# MONGO_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/clinicdb?retryWrites=true&w=majority
MONGO_URI=

# Optional: explicitly set the database name (overrides DB encoded in URI)
# DB_NAME=clinicdb

# If you want to run the server without MongoDB (for UI-only or offline),
# set SKIP_DB=true. Default is false.
# SKIP_DB=false

# Server port (optional, defaults to 5500)
# PORT=5500

# NODE_ENV=development
