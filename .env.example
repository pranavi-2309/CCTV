# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/clinic-tracker
# Or use MongoDB Atlas:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/clinic-tracker

# Server Configuration
PORT=8080
NODE_ENV=development

# Skip Database (for testing UI without MongoDB)
SKIP_DB=false

# Security (add in production)
# JWT_SECRET=your_jwt_secret_key_here
# SESSION_SECRET=your_session_secret_here
